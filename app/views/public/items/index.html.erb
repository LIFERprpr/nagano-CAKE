<h4>商品一覧</h4>(全<%= @items.count %>件)

<div>
  <% @items.each do |item| %>
  <!--商品画像のコードを記述する-->
  <%= attachment_image_tag item, :image %>
  <%= link_to customers_item_path(item) do %>
    <p><%= item.name %></p>
  <% end %>
    <p>¥<%= item.price %></p>
  <% end %>
</div>
<%= paginate @items %>